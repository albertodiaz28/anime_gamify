# shared-constants

Constants and pure helpers shared between backend and frontend.

## Exports
- `LEVEL_TABLE` — XP required per level (1..50, quadratic curve)
- `XP_PER_EPISODE = 10`
- `SKILLS_SEED` — initial skill catalog
- `computeLevel(xp): number`
- `xpToNext(level): number`
